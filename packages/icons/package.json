{
  "name": "@roo-ui/icons",
  "version": "0.39.0",
  "license": "MIT",
  "main": "dist/index.js",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "prepublish": "yarn build",
    "build": "babel src -d dist && node ./parseIcons.js",
    "build:watch": "onchange 'src/**/*.svg' -i -v -- yarn run build"
  },
  "devDependencies": {
    "glob": "^7.1.2",
    "onchange": "^4.0.0",
    "svgi": "^1.1.0",
    "svgo": "^1.0.5"
  }
}
