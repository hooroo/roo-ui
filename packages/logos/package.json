{
  "name": "@roo-ui/logos",
  "version": "0.57.1",
  "license": "MIT",
  "main": "dist/index.js",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "prepublish": "yarn build",
    "build": "babel src -d dist --copy-files",
    "build:watch": "yarn run build --watch"
  }
}
