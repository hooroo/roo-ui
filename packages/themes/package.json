{
  "name": "@roo-ui/themes",
  "version": "0.58.10",
  "license": "MIT",
  "main": "dist/index.js",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "prepublish": "yarn build",
    "build": "babel --root-mode upward src -d dist",
    "build:watch": "yarn run build --watch"
  }
}
