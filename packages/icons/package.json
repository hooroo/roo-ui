{
  "name": "@roo-ui/icons",
  "version": "0.19.1",
  "main": "dist/index.js",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "prepublish": "yarn build",
    "build": "babel src -d dist && node ./parseIcons.js"
  },
  "devDependencies": {
    "glob": "^7.1.2",
    "svgi": "^1.1.0",
    "svgo": "^1.0.5"
  }
}
