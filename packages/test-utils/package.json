{
  "name": "@roo-ui/test-utils",
  "version": "0.39.0",
  "main": "dist/index.js",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "prepublish": "yarn build",
    "build": "babel src -d dist",
    "build:watch": "yarn run build --watch"
  }
}
